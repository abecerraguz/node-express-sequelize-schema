{{> partials/navbars }}
    <div class="container vh-100 my-5 pt-5">
        <h1 class="h5">{{> partials/title }}</h1>
        <hr>
        <div class="table-responsive-sm">
          <div class="contentTable">
  <table class="table px-2 table-hover">
                <thead>
                    <tr>
                        <th scope="col">ID</th>
                        <th scope="col">Nombre</th>
                        <th scope="col">Apellido</th>
                        <th scope="col">Sexo</th>
                        <th scope="col">Fecha de Nacimiento</th>
                        <th scope="col">Ciudad</th>
                        <th scope="col">Estado</th>
                        <th scope="col">Celular</th>
                        <th scope="col">Acción</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each pacientes}}
                    <tr>
                        <td data-label="">{{this.pk_idPaciente}}</td>
                        <td data-label="">{{this.nombre}}</td>
                        <td data-label="">{{this.apellido}}</td>
                        <td data-label="">{{this.sexo}}</td>
                        <td data-label="">{{this.fechaNacimiento}}</td>
                        <td data-label="">{{this.ciudad}}</td>
                        <td data-label="">{{this.estado}}</td>
                        <td data-label="">{{this.telefono}}</td>
                        <td>
                            {{!-- // infoProducto(buscar.store_name, buscar.product_id, replaceQuotes(buscar.product_name), buscar.quantity) --}}
                            <button type="button" name="{{this.pk_idPaciente}}" class="btn btn-warning btn-sm" id="{{this.pk_idPaciente}}">
                                <i class="bi bi-pencil-square"></i> Editar
                            </button>
                             <button type="button" name="{{this.pk_idPaciente}}" class="btn btn-danger btn-sm" id="{{this.pk_idPaciente}}">
                                <i class="bi bi-trash"></i> Eliminar
                            </button>

                        </td>
                    </tr>
                    {{/each}}

                </tbody>
            </table>
          </div>
        
            </div>
    </div>
{{> partials/footer }}


